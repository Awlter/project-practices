mixin stylesheet_link_tag(src)
  link(rel="stylesheet" href=`/stylesheets/${src}.css`)

mixin script_include_tag(src, ...srcs)
  script(src=`/javascripts/${src}.js` type="text/javascript")
  each src in srcs
    script(src=`/javascripts/${src}.js` type="text/javascript")